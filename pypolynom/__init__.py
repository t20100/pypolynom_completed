

"""
Test module.

Libraries `pytest` and `pytest-cov` can be installed to easily test the
project with this command line.

.. code-block::

    # Test the project
    python3.5 -m pytest

    # Test coveraage with percentage of coverage per modules
    python3.5 -m pytest --cov pypolynom

    # Test coveraage with file annotation
    python3.5 -m pytest --cov pypolynom --cov-report annotate

"""